services:
  system-prompt-lab:
    build: .
    container_name: system-prompt-disclosure-lab
    ports:
      - "8501:8501"
    env_file:
      - .env.example
    extra_hosts:
      - "host.docker.internal:host-gateway"
